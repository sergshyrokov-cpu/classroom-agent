# Задание: ревью решений v64

Проведи независимое смысловое ревью изменений одной сессии. Ты этих правок не делал:
читай с нуля и проверяй написанное, а не предполагаемое.

**Что изменилось** — коммит `1fde653` в master (`git diff 4539608..1fde653`). Это итог
ревью v61–v63 (задание `docs/review-task-v63.md`, 19 находок):
- **отключённый Декан** — при верном пароле отдельное сообщение, неудачной попыткой не
  считается; термины «отключение учётной записи» и «временная блокировка входа» разведены,
  сброс пароля снимает только блокировку;
- **пароли и логины** — пароль Декана при создании учётной записи временный; логин
  Владельца 4–64 символа; «содержит логин» без учёта регистра, для email ещё и по части до
  `@`; пароль не длиннее 128 символов;
- **CSRF** — переведённая страница «страница устарела» при отказе antiforgery, для REST —
  тело API-6 через фильтр результата; страница ошибки в анонимном списке SC-4; в списке
  исключений только запросы не GET, колбэк Google OAuth описан в правиле о GET; запросы
  служебного канала — POST; TC-5 проверяет поведением; форма выгрузки в API-3;
- **cookie и HTTPS** — остальные cookie с атрибутами по умолчанию, но все `Secure`;
  приватный порт инсталляции по HTTP; Control Plane только HTTPS, без порта HTTP и HSTS;
  шаги с сертификатом в DC-2 и DC-3;
- **ключи Data Protection** — свой каталог у каждого хоста, вне базы и резервных копий;
- **workflow** — коммиты предлагает `story-orchestrator` (раздел Commit Offer);
  `COMPLETED` ставится при `/so:approve`, итоговый коммит сразу после; из
  `so/approve.md` и `story-orchestrator` убран `ARCHIVED`;
- **разрастание** — пересказы политики пароля и CSRF заменены ссылками на SC-2, SC-4,
  SC-7 (§2, таблица SC-4, BR-014, NFR-072, API-4, API-7, скиллы).

**Объём:** `trebovaniya.md` (авторитетный источник) и все файлы коммита. Кроме того —
файлы, которые эти решения затрагивают, но в коммит не попали: остальные скиллы в
`.claude/skills/` (особенно `spec-writer`, `openapi-designer`, `db-designer`), команды
`.claude/commands/so/`, `docs/workflow/state-schema.md`, `docs/workflow/artifact-paths.yaml`,
`docs/architecture/architecture.md`, `docs/architecture/persistence-conventions.md`,
`docs/product/*` (глоссарий, epic-map, product-vision), `docs/catalog/stories.yaml`.

**Механически проверено:** YAML `stage-map.yaml` и `stories.yaml` разбирается; все
идентификаторы правил, на которые есть ссылки, существуют; старые формулировки («5 failed
attempts → 15» в таблице SC-4, «per-host SameSite» у antiforgery-cookie, колбэк в списке
исключений, «учётная запись заблокирована») не найдены. Остальное проверь сам.

**Решения v61–v64 приняты владельцем продукта.** Не предлагай их пересмотреть. Находка —
это противоречие, расхождение или двусмысленность, а не несогласие с решением.

## Где искать в первую очередь

1. **Отключённый Декан — сочетания состояний.**
   - **Где сверять:** §2 (Декан), §9 «Модель ролей», §5 (категории аудита), SC-2, SC-11,
     BR-014, BR-026.
   - **Вопрос:** что видит отключённый Декан, у которого ещё и действует временная
     блокировка, при верном пароле? Какая категория аудита пишется при верном пароле
     отключённой учётной записи? Обязательная смена временного пароля — до или после
     проверки отключения?
   - **Подбор:** не позволяет ли «верный пароль не считается неудачной попыткой» отличить
     верный пароль от неверного у отключённой учётной записи обходом блокировки?
2. **Термины после разведения.**
   - **Вопрос:** не осталось ли в `trebovaniya.md` и производных документах «блокировки» в
     смысле отключения (или наоборот), в том числе в английском: `disable`, `lock`,
     `lockout`, `block`? Нужен ли термин в глоссарии?
3. **Временный пароль при создании учётной записи.**
   - **Где сверять:** §2, §4 Epic 6, §5 (аудит), BR-014, BR-026, персона Декана, US-012 в
     `stories.yaml` и `epic-map.md`.
   - **Вопрос:** сочетается ли с read-only (создание заблокировано, смена разрешена)? Нет ли
     места, где обязательная смена по-прежнему описана только после сброса?
4. **Логин Владельца и «содержит логин».**
   - **Вопрос:** логин Владельца не может содержать `@` — не лишняя ли для него оговорка про
     часть до `@`, и однозначно ли из текста, к кому она относится? Сочетается ли формат
     логина с AC-002 и AC-003 US-001? Совпадают ли 4, 64, 15 и 128 во всех местах?
5. **Ответ при отказе antiforgery.**
   - **Где сверять:** §8, SC-4 (обе таблицы), API-7, API-10, AD-9, TC-5, package-map, US-001
     AC-009–AC-011.
   - **Вопрос:** на каком языке страница ошибки для анонимного пользователя (язык школы по
     умолчанию или язык браузера)? Не противоречит ли «страница ошибки — анонимная» правилу
     SC-6 и перебирающему тесту TC-5? Однозначно ли «одна страница для всех форм» при
     других ошибках (404, 500)?
6. **Список исключений и тест TC-5.**
   - **Вопрос:** выполним ли TC-5 «от пользователя с разрешённой ролью» для эндпоинтов
     служебного канала, у которых роли нет, и для push-приёмника на приватном порту? Видит
     ли тестовый хост приватный порт?
7. **Выгрузка в API-3.**
   - **Где сверять:** API-2, API-3, API-4, API-5, API-8, SC-5, SC-11, скилл `openapi-designer`.
   - **Вопрос:** согласованы ли код ответа, ошибки, read-only (выгрузка разрешена) и аудит?
     Знает ли `openapi-designer` об этой форме?
8. **HTTP на приватном порту и HTTPS Control Plane.**
   - **Где сверять:** §8, §9, SC-2, SC-9, DC-1, DC-2, DC-3, DC-6, DC-11, `security-reviewer`.
   - **Вопрос:** не осталось ли мест, где HTTPS-редирект и HSTS описаны для всей
     инсталляции, а Control Plane — с HTTP? Корректна ли нумерация и порядок шагов DC-2?
9. **Ключи Data Protection.**
   - **Где сверять:** §8, §9 «Резервное копирование», SC-7, SC-13, DC-3, DC-8, DC-13.
   - **Вопрос:** что с каталогом ключей при выводе школы из эксплуатации и при переезде
     на новую `Installation`? Не противоречит ли «ключи не в резервных копиях» проверке
     восстановления раз в квартал и тексту «копируются все базы»?
10. **Коммиты workflow.**
    - **Где сверять:** `AGENTS.md` (Git Policy, Definition of Done п. 8–10), раздел Commit
      Offer и Output Format `story-orchestrator`, `stage-map.yaml`, `stages.md`,
      `artifact-lifecycle.md`, `state-schema.md`, команды `so/*.md`.
    - **Вопрос:** согласованы ли точки предложения коммита между собой и с `/so:reject`?
      Входит ли в итоговый коммит всё, что требует DoD п. 9 (трассировка файлов в
      `implementation_report`), если в нём ещё и артефакты workflow? Не осталось ли
      `ARCHIVED` в статусах workflow где-то ещё?
11. **Замена пересказов ссылками.**
    - **Вопрос:** не потерялась ли информация — особенно в NFR-072, BR-014, API-4 и
      чек-листе Step 6 и Step 7 `security-reviewer`: остаётся ли он проверяемым списком?
      Ведут ли ссылки туда, где правило действительно записано?
12. **Шапка и отметки.** Соответствует ли запись v64 тексту разделов? Стоят ли пометки
    «(v64)» там, где изменение сделано? Верны ли «Verified against v64» в `AGENTS.md` и
    «Aligned with trebovaniya.md v64» в US-001?

## Формат отчёта

Находки по убыванию важности. Для каждой:
- файл и строка;
- тип: противоречие / расхождение дублей / двусмысленность / разрастание / устаревшее или
  ошибка ссылки;
- в чём дефект;
- почему это важно;
- предлагаемое исправление.

Если решения нет, исправление — открытый вопрос, а не готовый ответ: в §7 для уровня
проекта, OD в US-001 для пробела уровня Story.

Важность, от высокой к низкой:
1. противоречие внутри `trebovaniya.md`;
2. расхождение, из-за которого реализация пойдёт не так: производный документ говорит
   иначе, чем требования, или два производных документа противоречат друг другу;
3. двусмысленность, которую можно прочитать двумя способами;
4. устаревшее или ошибка ссылки;
5. разрастание.

## Правила работы

- ничего не правь до моего подтверждения, в том числе после проверки;
- после отчёта идём по находкам по одной;
- в вариантах помечай лучшую практику «(Рекомендую)»;
- вопросы задавай обычным текстом;
- не придумывай решения за меня: если чего-то нет в требованиях, это открытый вопрос;
- номера новых правил ставь по порядку, без вставок вроде «TC-4a»;
- отвечай по-русски;
- коммит только по моей просьбе, прямо в master;
- workflow US-001 не запускай — это следующий шаг после ревью.
