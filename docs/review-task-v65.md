# Задание: короткая проверка правок v65

Проведи независимую смысловую проверку изменений одной сессии. Ты этих правок не делал:
читай с нуля и проверяй написанное, а не предполагаемое.

**Это не полное ревью.** Проверяются только правки v65 и то, что они затрагивают. Старые
места, которых v65 не касается, не ревьюй — если что-то бросилось в глаза, упомяни одной
строкой в конце отчёта.

**Что изменилось** — коммит `de270e6` в master (`git diff 00dfb77..de270e6`), 17 файлов. Это
итог ревью v64 (задание `docs/review-task-v64.md`, 17 находок):
- **вход в отключённую учётную запись Декана** — временная блокировка проверяется раньше
  отключения; верный пароль отключённой учётной записи счётчик не увеличивает и не
  сбрасывает; категория аудита «учётная запись отключена» — только при верном пароле;
  отключение проверяется до обязательной смены временного пароля;
- **«пароль не содержит логин»** — подстрока проверяется только для логина или части email
  до `@` не короче 4 символов, короче — только совпадение; оговорка про email перенесена к
  Декану (§2);
- **страница ошибки** — одна в каждом хосте на отказ antiforgery (400), 404 и 500, доступна
  без входа, на языке пользователя или языке по умолчанию; US-001 AC-001 её не перехватывает;
- **развёртывание** — корневой сертификат внутреннего центра и на устройствах Владельца;
  каталог ключей Data Protection удаляется при выводе школы; приватные маршруты привязаны к
  порту через `RequireHost`, тест — заголовком `Host`;
- **workflow** — оркестратор только предлагает коммит, коммитит агент по «коммить»; код и
  тесты прослеживаются в `implementation_report`, артефакты workflow — по путям
  `artifact-paths.yaml`; `COMPLETED` = одобрено, Done — после итогового коммита; `/so:reject`
  ставит `IN_PROGRESS`; убраны `ARCHIVED`, `archived_at` и пример со spec review;
- **прочее** — NFR-072 с исключением для колбэка OAuth; в SC-4 записано, что пишет колбэк;
  строка SC-2 в чек-листе `security-reviewer` перечисляет пункты; глоссарий: «Disabled
  account», «Sign-in lockout», запрет «block»; пометки «(v64)»; маркеры v65 в `AGENTS.md` и
  US-001.

**Механически проверено:** YAML `stage-map.yaml` и `stories.yaml` разбирается; `ARCHIVED`
остался только в пометках о том, что он выведен из употребления.

**Решения v61–v65 приняты владельцем продукта.** Не предлагай их пересмотреть. Находка —
это противоречие, расхождение или двусмысленность, а не несогласие с решением.

## Где искать

1. **Порядок проверок при входе Декана.**
   - **Где сверять:** §2 (Декан), §5 (аудит), §9 «Модель ролей», SC-2, SC-11, BR-014,
     `test-writer`, `security-reviewer`.
   - **Вопрос:** однозначен ли полный порядок — неизвестный логин, блокировка, пароль,
     отключение, смена временного пароля? Совпадает ли он во всех местах? Не противоречит ли
     «верный пароль не сбрасывает счётчик» тому, что после окончания блокировки отключённый
     Декан с верным паролем снова увидит своё сообщение? Реализуем ли порядок штатным
     ASP.NET Core Identity или требует своей логики — и сказано ли это там, где нужно?
2. **Порог 4 символа.**
   - **Вопрос:** однозначно ли, что «короче 4» относится к части до `@`, а полный email
     Декана проверяется на «содержит» всегда? Нет ли расхождения между §2, §9 и SC-2? Не
     задевает ли правило AC-006 US-001 (логин Владельца не короче 4)?
3. **Страница ошибки.**
   - **Где сверять:** §8, SC-4 (таблица анонимного доступа и новый пункт), SC-6, API-5,
     API-6, API-7, API-10, AD-9, TC-5, US-001 AC-001, AC-010, AC-011.
   - **Вопрос:** сочетается ли анонимная страница 404/500 с fallback-политикой и
     перебирающим тестом TC-5? REST-вызов при 404/500 получает тело API-6, а не страницу —
     это где-то сказано? Страница 500 и единственный `IExceptionHandler` (AD-9) — не два
     механизма на одно и то же?
4. **`RequireHost` и тест приватного порта.**
   - **Где сверять:** DC-6, TC-5, SC-4, SC-9, `dotnet-implementor`, `package-map.md`.
   - **Вопрос:** нет ли противоречия между «эндпоинт на отдельном Kestrel-порту» и
     «привязка сопоставлением хоста»? Защищает ли `RequireHost` от поддельного заголовка
     `Host` на публичном порту, если перед ним стоит reverse proxy?
5. **Трассировка файлов и коммиты.**
   - **Где сверять:** `AGENTS.md` (Git Policy, DoD п. 9–10), `story-orchestrator` (Commit
     Offer, Auto Mode, Prohibited Actions, Output Format), `stage-map.yaml`, `stages.md`,
     `artifact-lifecycle.md`, `artifact-paths.yaml`, `so/*.md`.
   - **Вопрос:** все ли файлы итогового коммита покрываются одним из двух способов
     трассировки (например, `stories.yaml`, `history.jsonl` — есть ли они в
     `artifact-paths.yaml`)? Не осталось ли мест, где оркестратор сам коммитит или где
     `COMPLETED` описан как «закоммичено»?
6. **Шапка и отметки.** Соответствует ли запись v65 тексту разделов? Стоят ли пометки
   «(v65)» там, где изменение сделано? Верны ли маркеры v65 в `AGENTS.md` и US-001?

## Формат отчёта

Находки по убыванию важности. Для каждой:
- файл и строка;
- тип: противоречие / расхождение дублей / двусмысленность / разрастание / устаревшее или
  ошибка ссылки;
- в чём дефект;
- почему это важно;
- предлагаемое исправление.

Если решения нет, исправление — открытый вопрос, а не готовый ответ: в §7 для уровня
проекта, OD в US-001 для пробела уровня Story.

Важность, от высокой к низкой:
1. противоречие внутри `trebovaniya.md`;
2. расхождение, из-за которого реализация пойдёт не так;
3. двусмысленность, которую можно прочитать двумя способами;
4. устаревшее или ошибка ссылки;
5. разрастание.

Если находок нет — так и скажи; не добирай мелочью.

## Правила работы

- ничего не правь до моего подтверждения, в том числе после проверки;
- после отчёта идём по находкам по одной;
- в вариантах помечай лучшую практику «(Рекомендую)»;
- вопросы задавай обычным текстом;
- не придумывай решения за меня: если чего-то нет в требованиях, это открытый вопрос;
- номера новых правил ставь по порядку, без вставок вроде «TC-4a»;
- отвечай по-русски;
- все решения вносятся одной новой версией `trebovaniya.md` после всех находок;
- коммит только по моей просьбе, прямо в master;
- workflow US-001 не запускай — это следующий шаг после проверки.
