# Задание на новую сессию (после v60)

Проект classroom-agent. Продолжаем после ревью AGENTS.md (закончено, последний
коммит ревью b361b9c). Это задание трижды проверено ревью (коммиты 9b14aba,
3769511 и коммит «Задание v60: третье ревью») — начинай с последней версии
master.

Прочитай сначала:
- заголовок trebovaniya.md (changelog, сейчас v60);
- trebovaniya.md §7 «Открытые вопросы»;
- AGENTS.md.

Задачи по порядку:

1. Проверь, что CLAUDE.md (@AGENTS.md) подгрузил AGENTS.md в эту сессию.
   Если нет — скажи мне, и выясним почему.

2. Решим вопрос 25 (защита от CSRF). Предложи варианты текстом, пронумеруй,
   лучший пометь «(Рекомендую)». Варианты должны отвечать на все вопросы:
   - применяется ли antiforgery-токен ко всем изменяющим формам Razor и к
     REST-эндпоинтам, вызываемым из того же UI (и как токен передаётся в REST —
     например, заголовком);
   - нужен ли токен анонимным формам: вход Декана, вход Владельца, первый запуск
     Control Plane (защита от login CSRF);
   - вход Админа через Google: колбэк OAuth явно исключить из antiforgery —
     токен в редирект от Google не вставить, защита — параметр `state` и
     correlation-cookie; требует ли токен кнопка запуска входа «Войти через
     Google»;
   - служебный канал Control Plane ⇄ инсталляция (push-приёмник, проверка
     легитимности, проверка входа Админа) — браузера и cookie там нет; явно
     исключить его из antiforgery, защита — сетевая изоляция (SC-9);
   - значение `SameSite` сессионного cookie **для каждого хоста отдельно**:
     в инсталляции вход Админа идёт через редирект с Google, и `Strict` там
     ломает первую страницу после входа; в Control Plane OAuth нет;
   - HTTPS и атрибут `Secure` у cookie (сессионного и antiforgery) — тоже для
     каждого хоста: в инсталляции HTTPS есть (§9); для Control Plane в
     требованиях записаны только приватная сеть и VPN/туннель (v35), про HTTPS
     ничего. Если в Control Plane HTTPS нет — cookie без `Secure` (иначе браузер
     его не отправит), трафик защищает VPN/туннель; записать это в §9 рядом с v35;
   - правило «GET ничего не меняет»: всё, что изменяет данные (в том числе
     кнопка «Синхронизировать», выход из системы, выбор языка — US-039),
     выполняется только POST/PUT/DELETE. Варианты должны учесть GET-запросы,
     которые по требованиям уже пишут: колбэк Google OAuth (создание `AppUser` Админа при первом входе, аудит входа — SC-3,
     SC-11) и выгрузку журнала или отчёта (аудит — SC-11, BR-026); для выгрузки
     ответить, каким методом она выполняется — GET или POST.

   Решение оформи новой версией trebovaniya.md (v61, со строкой в changelog):
   текст решения — в §8 (рядом с «Токен сессии — в httpOnly cookie»), пункт 25
   из §7 убрать, номер не переиспользовать. Подраздел «### Решить» останется
   пустым — удали заголовок. Перенеси решение в производные документы:
   - docs/architecture/security-conventions.md — SC-2 и/или SC-4; исключения из
     antiforgery (колбэк OAuth, служебный канал и что ещё даст решение) —
     закрытым списком по образцу таблицы анонимных эндпоинтов SC-4, с защитой,
     заменяющей токен, у каждого; исключение вне списка — Critical;
   - docs/architecture/api-conventions.md — API-7 (antiforgery-токен в REST,
     если решение его вводит), API-4 (правило «GET ничего не меняет» и его
     исключения), API-2 (метод выгрузки — GET или POST);
   - docs/product/non-functional-requirements.md — NFR-072;
   - docs/architecture/deployment-conventions.md — DC-1 / DC-6 (HTTPS или его
     отсутствие у Control Plane);
   - docs/architecture/testing-conventions.md — TC-5 (тесты: изменяющий запрос
     без токена отклоняется; тест перебирает эндпоинты каждого хоста и падает
     на исключении из antiforgery вне закрытого списка; изменяющее действие по
     GET — например, выход или выбор языка — не выполняется);
   - .claude/skills/security-reviewer/SKILL.md — строку в таблице Step 6 и
     заменить заглушку «not yet defined» в Step 7;
   - .claude/skills/test-writer/SKILL.md — список обязательных тестов по TC-5;
   - .claude/skills/dotnet-implementor/SKILL.md — список правил безопасности
     для реализации (antiforgery, `SameSite`, GET без изменений);
   - docs/architecture/package-map.md — пространства имён `Security` обоих
     хостов — `Web` (строка ~83) и `ControlPlane` (строка ~104): настройка
     antiforgery, cookie и закрытый список исключений из antiforgery;
   - docs/stories/US-001-owner-first-run-setup.md — затрагивается (формы первого
     запуска и входа — изменяющие и анонимные). Добавь AC-011 «Изменяющие формы
     защищены от CSRF»: формы первого запуска и входа Владельца без
     antiforgery-токена отклоняются; `SameSite` и `Secure` у cookie Control
     Plane — по решению v61. Отметка сейчас «Aligned with trebovaniya.md v53»:
     прежде чем ставить v61, сверь Story с изменениями v54–v60 по changelog и
     скажи мне, если что-то её касается;
   - AGENTS.md — отметку «Verified against v60» на v61.
   Коммить и пушь в master.

3. Реши OD-001 из US-001 (политика пароля и блокировки входа Владельца: минимальная
   длина, сложность, lockout). Он блокирует HUMAN_SPEC_APPROVAL — вторую стадию
   workflow. Тот же формат: варианты текстом, пронумеровать, лучший пометить
   «(Рекомендую)». Отдельно спроси меня, решать ли заодно политику пароля и
   блокировки входа Декана: в SC-4 записано «Identity lockout after failed
   attempts», но числа попыток и длительности нет ни в trebovaniya.md, ни в SC.
   Если решаем — те же варианты рядом (у Декана своя специфика: сброс Админом и
   обязательная смена после него), перенос в §2, SC-2, SC-4,
   docs/product/business-rules.md (рядом с правилом о сбросе и смене пароля
   Декана, строки ~64–67), а ссылки в трёх скиллах ниже — «для Владельца и
   Декана — SC-2». Если нет — новый
   пункт 26 в §7 trebovaniya.md (в той же v62) под заголовком «### Решить» (после
   «### Проверить при внедрении»; заголовка к этому моменту нет — создай его
   заново) и ссылка на него в `blocked_by` у
   US-012 в docs/catalog/stories.yaml. Решение перенеси:
   - trebovaniya.md (v62) — текст решения в §9, рядом со входом Владельца;
   - docs/stories/US-001-owner-first-run-setup.md — OD-001 resolved,
     AC-005/006/008, отметка версии;
   - docs/architecture/security-conventions.md — SC-2 (пункт про Владельца:
     длина, сложность, блокировка) и SC-4 (строка «lockout policy open» в
     таблице анонимных эндпоинтов);
   - .claude/skills/security-reviewer/SKILL.md — в примерах открытых вопросов
     (строка ~332) заменить «US-001 OD-001» на «`trebovaniya.md` §7, п. 26»,
     если политику Декана не решаем, иначе просто убрать; к запрету придумывать политику
     пароля (строка ~364) добавить ссылку «для Владельца — SC-2»; в строку SC-2
     таблицы Step 6 (строка ~588) добавить проверку длины, сложности и
     блокировки пароля Владельца по SC-2;
   - .claude/skills/dotnet-implementor/SKILL.md — та же ссылка к запрету
     (строка ~632);
   - .claude/skills/test-writer/SKILL.md — та же ссылка к запрету придумывать
     политику пароля (строки ~407–412);
   - AGENTS.md — отметку «Verified against v61» на v62.
   Коммить и пушь в master.

4. Предложи, как начать US-001 через workflow (/so:start). SDK для старта не
   нужен: стадии SPECIFICATION → HUMAN_SPEC_APPROVAL → API_DESIGN → DB_DESIGN
   документные (docs/workflow/stage-map.yaml). Отдельно проверь, установлен ли
   .NET 10 SDK (dotnet --list-sdks) — он понадобится с TEST_WRITING. Если не
   установлен — скажи, что поставить. В любом случае выпусти v63 trebovaniya.md
   (строка в changelog, коммит и пуш в master): в §8 убери устаревшую фразу
   «Учесть при переносе SDLC-обвязки из customer-portal-NET: он на .NET 8,
   скрипты и таргеты потребуют правки» (обвязка уже перенесена и вычищена); если
   SDK 10 установлен — убери и фразу «На машине разработки сейчас стоит SDK 9.x —
   SDK 10 нужно доставить». Отметку в AGENTS.md подними до v63.

   Коммиты workflow: после /so:start и после каждой документной стадии
   (SPECIFICATION, HUMAN_SPEC_APPROVAL, API_DESIGN, DB_DESIGN) предлагай мне
   коммит состояния workflow и артефактов, а делай его только по моему явному
   «коммить». Скиллы сами не коммитят. Код Story коммитится, как и раньше, только
   после HUMAN_PR_APPROVAL. Перед первым таким коммитом допиши это правило в Git
   Policy в AGENTS.md (отдельным коммитом, по моему подтверждению текста).

Ни один скилл workflow ещё ни разу не запускался: dotnet-implementor,
security-reviewer, test-writer и story-orchestrator только что переписаны, а
spec-writer, openapi-designer и db-designer перенесены из customer-portal-NET,
который тоже не запускался. На первой Story следи за поведением всех семи
(первыми сработают story-orchestrator, spec-writer, openapi-designer и
db-designer) и сообщай о расхождениях со stage-map.yaml.
