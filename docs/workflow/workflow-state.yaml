# Execution state of the active story-delivery workflow.
# Schema: docs/workflow/state-schema.md
# Writer: story-orchestrator ONLY. Stage Skills never write this file.
version: 1
story: US-007
workflow: story-delivery
current_stage: DB_DESIGN
previous_stage: API_DESIGN
last_completed_stage: API_DESIGN
status: IN_PROGRESS
attempt: 1
last_invoked_skill: openapi-designer
last_result:
  verdict: NOT_APPLICABLE
  stage: API_DESIGN
  recorded_at: 2026-09-19T12:25:46Z
last_artifacts:
  - type: api_design
    path: docs/designs/api/US-007-api-design.md
    version: 1
pending_human_gate: null
blocking_issues: []
non_blocking_findings:
  - "API_DESIGN: NOT_APPLICABLE, optional_when met. The approved Specification v2 states in sections 1, 7 (S-08), 8 and 10 that US-007 adds no endpoint and that the refusal carries no HTTP concept; the HTTP 409 mapping of API-5 is deferred to US-008 by OD-001 (resolved). The decision and its reason are recorded in docs/designs/api/US-007-api-design.md (v1)."
  - "API_DESIGN: no docs/designs/api/US-007-openapi.yaml exists, deliberately - a contract describing zero operations would assert a surface this Story does not have. DB_DESIGN lists `openapi` among its inputs; that input's answer lives in the api_design document. Its absence is a recorded decision, not a missing artifact, and must not be read as one."
  - "API_DESIGN: recorded for US-008 - ReadOnlyModeException maps to HTTP 409 with the API-6 body, and the user-visible message is built from the reason carried as data, never by parsing the fixed English Message (NFR-073, AC-002)."
  - "API_DESIGN: existing contracts confirmed unchanged - the installation's private port (liveness, readiness in read-only mode still HTTP 200 'degraded', the push receiver), the legitimacy-check service channel, the status push, the Control Plane pages, and the installation's public port that still answers 404."
  - "HUMAN_SPEC_APPROVAL approved by Serhii SHYROKOV (/so:approve) at 2026-09-19T12:17:40Z: specification v2 and open decisions v2 APPROVED. OD-002 resolved as option 3 - Application throws and Infrastructure registers the decorators around IReadOnlyModeGuard and IUnitOfWork that write the Warning line and rethrow; no NuGet package is added to Application, no logging port is introduced, package-map.md is unchanged and the US-005 test ProjectReferenceTests.FrameworkFreeProjects_ReferenceNoPackage keeps passing unchanged (Story AC-011). Interpretations I-1 ... I-12 accepted."
  - "SPECIFICATION: OD-001 carried from the Story keeping its id, resolved by the Owner on 2026-09-19 (option 1). API-5 is NOT satisfied end to end after this Story - the refusal stops at Application and the HTTP 409 mapping with the translated message arrives with US-008."
  - "SPECIFICATION: the enforcement mechanism is two-layered - IReadOnlyModeGuard in Application as the primary point (refusal before any repository, port or transaction, AC-001/AC-006) plus a commit backstop, an IUnitOfWork decorator in Application that refuses in read-only mode any commit no use case declared a permitted service write (AC-004). The BR-026 list is one enum of four members with a static registry of the use cases performing them; the structural test enumerates write paths and Google paths and is proven against synthetic violations (AC-007)."
  - "SPECIFICATION: this is a mechanism Story - no endpoint, no screen, no entity and no migration. DB_DESIGN should expect the same answer API_DESIGN gave, and must not add an entity or a migration to fill the stage."
  - "SPECIFICATION: trebovaniya.md section 7 has no open item this Story depends on - items 10 and 14 are Google questions, item 26 is documentation."
started_at: 2026-09-19T12:15:12Z
updated_at: 2026-09-19T12:25:46Z
completed_at: null
