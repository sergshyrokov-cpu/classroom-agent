namespace ClassroomAgent.ControlPlane.Persistence;

/// <summary>Outcome of an audited action, stored as <c>succeeded</c> or <c>refused</c>.</summary>
public enum AuditOutcome
{
    Succeeded,
    Refused,
}
